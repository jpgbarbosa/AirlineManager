package bookings;

import common.Flight;
import common.Operator;

public class CharterBooking extends Booking {
	
	/* The constructor. */
	public CharterBooking(Flight flight, Operator client){
		super(flight,client);
	}
	
}
